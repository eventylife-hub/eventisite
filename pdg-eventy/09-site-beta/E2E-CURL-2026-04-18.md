# Tests E2E curl — 2026-04-18

## Backend API Scaleway (`https://api.eventylife.fr`)

| Endpoint | HTTP | Résultat |
|---|---|---|
| `GET /api/health` | **200** | ✅ `{"status":"ok","uptime":421104s,"version":"1.0.0"}` |
| `GET /api/travels` | **200** | ⚠️ **Base vide** — `{"success":true,"data":{"data":[],"nextCursor":null,"total":0}}` |
| `GET /api/admin/travels` | **401** | ✅ auth requise (attendu) |
| `POST /api/admin/seed-travels` | **404** | ❌ route pas encore déployée (commit `claude/seed-travels-route` prêt à pull) |

**Bloquant** : La base prod est vide → aucune fiche voyage n'a de contenu réel. Tous les 3 portails (Client/Pro/Admin) affichent zéro donnée jusqu'à l'exécution du seed.

## Frontend Vercel (`https://www.eventylife.fr`)

Test des 5 flux critiques — tous protégés par un **gate mot de passe site** (`/site-password`) :

| Flux | HTTP final | Redirige vers | Verdict |
|---|---|---|---|
| `/voyages/marrakech-express` | 200 | `/site-password?next=/voyages/marrakech-express` | ⚠️ **Gate privé beta** |
| `/pro/dashboard` | 200 | `/site-password?next=/pro/dashboard` | ⚠️ Gate |
| `/admin/` | 200 | `/site-password?next=/admin` | ⚠️ Gate |
| `/maisons/hebergement` | 200 | `/site-password?next=/maisons/hebergement` | ⚠️ Gate |
| `/client/` | 200 | `/site-password?next=/client` | ⚠️ Gate |

Toutes les pages répondent 200 avec la même payload (22 977 octets) = écran de saisie mot de passe. Impossible de tester le contenu réel sans password côté curl.

## Actions

1. **Fournir le mot de passe site** → relancer les 5 curl avec cookie `site-auth` pour vérifier vrai contenu
2. **Exécuter le seed** (cf. [`SEED-PROD-RUNBOOK.md`](./SEED-PROD-RUNBOOK.md)) — sans ça, fiche Marrakech = vide
3. Option : désactiver temporairement le gate via feature flag Vercel pour tests E2E robots
